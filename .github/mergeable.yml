version: 2
mergeable:
  - when: pull_request.*, pull_request.*, pull_request_review.*, pull_request_review.*, issues.*, issues.*  # can be one or more
    validate:
      # #list of validators. Specify one or more.
    -  do: label
       no_empty:
          enabled: true # Cannot be empty when true.
          #message: 'Needs QA Approval'
       must_include:
          regex: 'QA Approved'
          message: 'Approved by QA team'
       must_exclude:
          regex: 'Need QA'
          message: 'Need be tested by QA'
